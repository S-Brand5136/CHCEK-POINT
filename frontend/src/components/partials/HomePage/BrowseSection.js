import React from 'react';

import { Typography } from 'antd';

const BrowseSection = () => {
  return (
    <section class='browse-section'>
      <Typography.Title level={1}>Browse Games</Typography.Title>
    </section>
  );
};

export default BrowseSection;
